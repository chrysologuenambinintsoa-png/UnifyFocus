id: auto-1781822746974
name: Implement Settings Menu with API Integration
description: Workflow to make the settings menu functional with the real API and routing.
version: 1.0.0
tags:
  - auto-generated
source: auto
isAutoGenerated: true
createdAt: '2026-06-18T22:45:46.974Z'
updatedAt: '2026-06-18T22:45:46.974Z'
steps:
  - id: analyze-requirements
    name: Analyze Requirements
    description: Understand the requirements for the settings menu and API integration.
    requiresApproval: false
    onFail: abort
    agent: developer
    prompt: |-
      Understand the requirements for the settings menu and API integration.

      <!-- STEP_COMPLETE -->
    artifacts:
      - requirements-document
    reasoning: This step ensures clarity on what needs to be implemented.
  - id: design-implementation-plan
    name: Design Implementation Plan
    description: Create a plan for integrating the settings menu with the API and routing.
    requiresApproval: true
    onFail: abort
    agent: developer
    prompt: |-
      Create a plan for integrating the settings menu with the API and routing.

      ## Inputs from previous steps

      **Analyze Requirements** artifacts:
        - {{steps.analyze-requirements.requirements-document}}

      <!-- STEP_COMPLETE -->
    artifacts:
      - implementation-plan
    reasoning: A clear plan helps in organizing the implementation process.
  - id: implement-code
    name: Write Code
    description: Develop the code to connect the settings menu with the API and set up routing.
    requiresApproval: false
    onFail: abort
    agent: developer
    prompt: |-
      Develop the code to connect the settings menu with the API and set up routing.

      ## Inputs from previous steps

      **Analyze Requirements** artifacts:
        - {{steps.analyze-requirements.requirements-document}}

      **Design Implementation Plan** artifacts:
        - {{steps.design-implementation-plan.implementation-plan}}

      <!-- STEP_COMPLETE -->
    artifacts:
      - source-code
    reasoning: This is the core step where the actual functionality is implemented.
  - id: write-tests
    name: Write Tests
    description: Create unit and integration tests for the settings menu functionality.
    requiresApproval: false
    onFail: abort
    agent: developer
    prompt: |-
      Create unit and integration tests for the settings menu functionality.

      ## Inputs from previous steps

      **Analyze Requirements** artifacts:
        - {{steps.analyze-requirements.requirements-document}}

      **Design Implementation Plan** artifacts:
        - {{steps.design-implementation-plan.implementation-plan}}

      **Write Code** artifacts:
        - {{steps.implement-code.source-code}}

      <!-- STEP_COMPLETE -->
    artifacts:
      - test-cases
    reasoning: Testing ensures that the implementation works as expected.
  - id: self-review-summary
    name: Self-Review & Summary
    description: Review the implementation and tests, and summarize the work done.
    requiresApproval: true
    onFail: abort
    agent: developer
    prompt: |-
      Review the implementation and tests, and summarize the work done.

      ## Inputs from previous steps

      **Analyze Requirements** artifacts:
        - {{steps.analyze-requirements.requirements-document}}

      **Design Implementation Plan** artifacts:
        - {{steps.design-implementation-plan.implementation-plan}}

      **Write Code** artifacts:
        - {{steps.implement-code.source-code}}

      **Write Tests** artifacts:
        - {{steps.write-tests.test-cases}}

      <!-- STEP_COMPLETE -->
    artifacts:
      - review-summary
    reasoning: Self-review helps catch any issues before final submission.
