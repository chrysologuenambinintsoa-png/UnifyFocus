import React from "react";
import SupportPage from "@/components/views/support-page";

export const metadata = {
  title: "Contact Support - UnifyFocus",
  description: "Page de contact support UnifyFocus",
};

export default function Page() {
  return (
    <main>
      <SupportPage />
    </main>
  );
}
